# HTTP Adapter Boundary

This directory is the home of `HTTPAdapterProtocol` implementations.

Current recorded implementation:
- `URLSessionHTTPClient.swift`

Current phase:
- skeleton only
- implementation moved out of `Core`
- this does not represent final platform delivery
- no platform UI is included here
